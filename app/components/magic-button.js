// Re-exporting component:magic-button in the 'app' namespace
// makes it immediately available for use in the consuming app's
// templates.
//
// Also makes it available for extension in the consuming app.
export { default } from 'magic-button/components/magic-button';
